mnist:
  dataset: MNIST
  total_rounds: 15
  attack_start_round: 5
  local_training_epochs: 1
  batch_size: 256
  learning_rate: 0.1
  momentum: 0.9
  lr_decay: 0.95
  num_candidates: 2
  alpha: 0.5
  verifier_pool_size: 20
  initial_attacker_ratio: 0.7
  committee_size: 5
  stack_reward: 0.4
  rewards:
    verifier: 0.1
    aggregator: 0.2
    provider: 0.05
  slash_penalty: "full"
